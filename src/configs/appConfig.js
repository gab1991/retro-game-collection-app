const appConfig = {
  assetsMapping: {
    gamepadMap: {}
  },

  GameSelector: {
    defaultPage: 1,
    gamesPerRequest: 20,
    defaultOrdering: '-rating',
    ordering: ['name', 'released', 'rating']
  }
};

export default appConfig;
